<h2>List of ratings</h2>
<%= link_to "new rating", new_rating_path %>
<ul>
  <% @ratings.each do |rating| %>
    <li><%= render rating %> <%= button_to 'delete', rating_path(rating.id), method: :delete, data: {turbo_confirm: "Are you sure?"} %></li>
  <% end %>
</ul>